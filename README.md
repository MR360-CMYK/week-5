# Capstone Starter App

This is the starter Flask application for the capstone project.

## Setup

1. Clone the repository.
2. Create a virtual environment: `python -m venv venv`
3. Activate it: `venv\Scripts\activate` (Windows) or `source venv/bin/activate` (macOS/Linux)
4. Install dependencies: `pip install -r requirements.txt`
5. Run the app: `python app.py`
6. Open http://127.0.0.1:5000/

## Routes

- `/` : Home page
- `/hello` : Hello World

## Team Setup Verification

- [ ] Repository cloned successfully by all members
- [ ] Dependencies installed successfully for all members
- [ ] Starter app runs successfully for all members
- [ ] Basic route works for all members
- [ ] README contains clear setup steps
